<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlantsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.PlantsApp" parent="Base.Theme.PlantsApp" />
    <style name="CustomAppBarStyle" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryDark">@color/dark_main</item>
        <item name="colorAccent">@color/main</item>
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Optional: transparent status bar -->
        <item name="android:windowActionBar">false</item> <!-- Disable the default action bar -->
        <item name="android:windowNoTitle">true</item> <!-- Remove title bar -->
    </style>

</resources>